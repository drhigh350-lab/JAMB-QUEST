import { JambCalculator } from "@/components/JambCalculator";

export default function CalculatorFixture() {
  return <main className="page-shell" style={{ paddingTop: 48 }}><JambCalculator initialOpen /></main>;
}
