# Owner-Provided Drive Extraction Scope

The connected Drive folder `Free JAMB Past Questions and Answers` has a broader current set of owner-provided PDFs than the earlier six-file inventory recorded. The confirmed batch contains **28 PDFs**: **26 subject-labelled candidates** across Use of English (7), Biology (6), Chemistry (6), and Physics (7); one mixed-subject JAMB simulation; and one Mathematics PDF. The Mathematics file is excluded from this four-subject JAMB Quest scope.

| Batch | Source count | Treatment |
|---|---:|---|
| Use of English | 7 | Extract and stage with file-level provenance. |
| Biology | 6 | Extract and stage with file-level provenance, separate from the supplied Biology Markdown. |
| Chemistry | 6 | Extract and stage with file-level provenance. |
| Physics | 7 | Extract and stage with file-level provenance. |
| Mixed JAMB simulation | 1 | Profile first, then split only where subject headings are explicit. |
| Mathematics | 1 | Exclude from the current app scope; do not download into the JAMB Quest game bank. |

The folder will be handled as a private, owner-provided source collection. Extraction must retain each original file name and Drive file ID, assign a subject only where the file name or parsed content supports it, deduplicate records, and preserve a provenance status of `owner-provided / verification pending`. Nothing in this collection will replace the 1,000 original model questions or be represented as official JAMB wording without independent verification.

The confirmed source filenames and Drive IDs are retained in the Drive listing captured at `terminal_full_output/2026-08-13_12-43-56_604111_1129.txt`. The initial six-file extraction plan is superseded by this subject-based batch. The high-volume JAMB REMIX source files alone account for roughly 232 MB of the owner-provided collection, so parsing will be staged and deduplicated before any database import.
